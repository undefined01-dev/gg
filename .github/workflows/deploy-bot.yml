 ```yaml
 name: Deploy Discord Bot

 on:
   push:
     branches: [main]

 jobs:
   deploy:
     runs-on: ubuntu-latest
     steps:
       # ... (other steps like installing dependencies) ...

       - name: Start the bot
         run: node bot.js
         env:
           TOKEN: ${{ secrets.TOKEN }} 
 ```